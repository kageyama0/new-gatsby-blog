import { extendTheme } from '@chakra-ui/react'

const theme = {
  colors: {
    primary: '#4FBDBA',
    secondary: '#35858B',
    background: '#F7F7F7',
    black: '#072227'
  },
}

export default extendTheme(theme)

export const baseTheme = {};
